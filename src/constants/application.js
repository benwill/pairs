// Time it takes for the card to transition -> this should be in sync with the css animation
export const TRANSITION_TIME = 500;

// We want the clock to tick every second
export const TICK_TIME = 1000;

// Shows card numbers
export const CHEAT = false

// number of high scores to show
export const NUMBER_OF_HIGH_SCORES = 5;

